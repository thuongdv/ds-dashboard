# =============================================================================
# Test Utilities - Environment Configuration Template
# =============================================================================
#
# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace 'null' with your actual values
# 3. Never commit .env file to version control
#
# =============================================================================

# -----------------------------------------------------------------------------
# DWS Dashboard Configuration
# -----------------------------------------------------------------------------
# The base URL of your DWS instance (without trailing slash)
DWS_URL=null

# Email address for DWS authentication
DWS_EMAIL=null

# Password for DWS authentication
DWS_PASSWORD=null

# -----------------------------------------------------------------------------
# Reports Configuration
# -----------------------------------------------------------------------------
# Directory path where test reports will be saved
# Use absolute path or relative path from project root
# Example: ./dws-report/reports or C:/Users/username/reports
REPORTS_PATH=null

# -----------------------------------------------------------------------------
# Jira Integration Configuration
# -----------------------------------------------------------------------------
# Base URL of your Jira instance
JIRA_BASE_URL=null

# Jira API token (generate from: https://id.atlassian.com/manage-profile/security/api-tokens)
JIRA_API_TOKEN=null

# Email address associated with your Jira account
JIRA_EMAIL=null

# Jira project key (e.g., PROJ, TEST, DEV)
JIRA_PROJECT=null

# -----------------------------------------------------------------------------
# Optional: Test Execution Settings (Uncomment and set values as needed)
# -----------------------------------------------------------------------------
# Number of test results to collect per queue (default: 1)
# You can override this when running tests:
# NUMBER_OF_QUEUE_RESULTS=5 npx playwright test
# NUMBER_OF_QUEUE_RESULTS=10

# Number of results per API page request (default: 100)
# Increase for fewer API calls, decrease if timeouts occur
# NUMBER_OF_TESTS_PER_QUEUE=150

# Logging level: error, warn, info, debug (default: info)
# LOG_LEVEL=info

# =============================================================================
# Security Notes:
# - Keep this file secure and never share it publicly
# - Use different credentials for each environment (dev, staging, prod)
# - Rotate passwords and tokens regularly
# - Consider using a secrets management service for production
# =============================================================================
